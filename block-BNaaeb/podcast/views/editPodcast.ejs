<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Podcast</title>
</head>
<body>
  <!-- Include the header from the homepage -->
  <% include partials/header %>

  <!-- Edit Podcast Form -->
  <div class="edit-podcast">
    <h2>Edit Podcast</h2>
    <form action="/podcasts/edit/<%= podcast._id %>" method="post">
      <div>
        <label for="title">Title:</label>
        <input type="text" id="title" name="title" value="<%= podcast.title %>" required>
      </div>
      <div>
        <label for="description">Description:</label>
        <textarea id="description" name="description" required><%= podcast.description %></textarea>
      </div>
      <div>
        <label for="category">Category:</label>
        <select id="category" name="category" required>
          <option value="free" <% if (podcast.category === "free") { %>selected<% } %>>Free</option>
          <option value="vip" <% if (podcast.category === "vip") { %>selected<% } %>>VIP</option>
          <option value="premium" <% if (podcast.category === "premium") { %>selected<% } %>>Premium</option>
        </select>
      </div>
      <div>
        <button type="submit" class="btn btn-blue">Update</button>
        <a href="/admin/dashboard" class="btn btn-green">Cancel</a>
      </div>
    </form>
  </div>

  <!-- Include the footer from the homepage -->
  <% include partials/footer %>
</body>
</html>
